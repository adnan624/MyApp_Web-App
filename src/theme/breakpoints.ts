/** Two-column / hero + media layouts */
export const LAYOUT_TABLET = 768;

/** Wide feature grid */
export const LAYOUT_DESKTOP = 1100;

/** Centered narrow content (forms, lists, cards) */
export const CONTENT_WIDE = 640;
